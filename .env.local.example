# ══════════════════════════════════════════════════════════════════════
#  .env.local — Garage Mendonça — Configuration locale / Vercel
#
#  IMPORTANT : Ce fichier ne doit JAMAIS être commité dans le repo.
#  Copier ce fichier en .env.local et compléter les valeurs.
#
#  .env.local est dans .gitignore — vos clés restent sur votre machine.
#
#  Sur Vercel : Project Settings → Environment Variables → ajouter
#  les mêmes clés (scope : Production + Preview + Development).
# ══════════════════════════════════════════════════════════════════════

# ── Identifiant du garage ──────────────────────────────────────────
# UUID de la ligne dans la table `garages` (colonne `id`).
# Valeur seed par défaut : 00000000-0000-0000-0000-000000000001
# En production : remplacer par l'UUID réel généré par Supabase.
NEXT_PUBLIC_GARAGE_ID=00000000-0000-0000-0000-000000000001

# ── Mode d'exécution ───────────────────────────────────────────────
# NEXT_PUBLIC_DEMO_MODE
#   true  → données statiques embarquées, aucun appel Supabase
#   false → Supabase obligatoire (erreur si non configuré)
#   non défini + Supabase absent → auto-bascule en mode démo
NEXT_PUBLIC_DEMO_MODE=false

# ── URL du site (metadataBase Next.js — OpenGraph / Twitter cards) ──
# Doit inclure le protocole, sans slash final.
# En production : https://www.garagemendonca.com
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# ── Supabase ───────────────────────────────────────────────────────
# NEXT_PUBLIC_SUPABASE_ENABLED
#   false → désactive Supabase même si URL+key présentes (override)
#   non défini → activé si URL + clé présentes
# NEXT_PUBLIC_SUPABASE_ENABLED=false

# URL de votre projet Supabase
# Supabase Dashboard → Settings → API → Project URL
NEXT_PUBLIC_SUPABASE_URL=https://VOTRE_PROJECT_ID.supabase.co

# Clé anon (publique) — utilisée côté client pour les lectures RLS
# Supabase Dashboard → Settings → API → anon / public
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# Clé service-role (SECRÈTE) — utilisée côté serveur uniquement (Server Actions)
# Supabase Dashboard → Settings → API → service_role / secret
# NE JAMAIS EXPOSER CÔTÉ CLIENT — jamais de NEXT_PUBLIC_ prefix ici
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# URL publique du bucket Supabase Storage pour les logos garage (optionnel)
# Format : https://VOTRE_PROJECT_ID.supabase.co/storage/v1/object/public/garage-logos
# NEXT_PUBLIC_SUPABASE_LOGOS_URL=https://VOTRE_PROJECT_ID.supabase.co/storage/v1/object/public/garage-logos

# ── Email — Resend ─────────────────────────────────────────────────
# Resend Dashboard → API Keys
RESEND_API_KEY=re_...

# Adresse expéditeur — DOIT être un domaine vérifié dans Resend
# Format : "Nom Garage <email@votre-domaine.com>"
RESEND_FROM=Garage Mendonça <contact@garagemendonca.com>

# Adresse de réception des notifications (email du gérant)
GARAGE_EMAIL=contact@garagemendonca.com

# ── Analytics — Google Tag Manager / GA4 ─────────────────────────
# GTM_ID : format GTM-XXXXXXX (depuis tagmanager.google.com)
# Laisser vide pour désactiver GTM entièrement.
NEXT_PUBLIC_GTM_ID=GTM-XXXXXXX

# GA_ID : format G-XXXXXXXXXX (optionnel si GTM charge GA4)
# Laisser vide si GA4 est chargé via GTM.
# NEXT_PUBLIC_GA_ID=G-XXXXXXXXXX

# Google Consent Mode v2 (défaut : activé)
#   false → désactive la gestion du consentement Google
# NEXT_PUBLIC_GOOGLE_CONSENT_MODE=true

# ── Cloudflare Turnstile (protection anti-bot, optionnel) ─────────
# Cloudflare Dashboard → Turnstile → votre site
# Laisser vide pour désactiver le challenge Turnstile sur la page login.
# NEXT_PUBLIC_CLOUDFLARE_TURNSTILE_SITE_KEY=0x4AAAAAAA...
# CLOUDFLARE_TURNSTILE_SECRET_KEY=0x4AAAAAAA...

# ── Étapes pour connecter Supabase ────────────────────────────────
#
# 1. Créer un projet sur https://supabase.com
# 2. Exécuter les migrations SQL dans supabase/migrations/ (ordre croissant)
# 3. (Optionnel) Exécuter supabase/seed.sql pour les données de test
# 4. Créer les buckets Storage (Public, CDN activé) :
#      vehicle-images
#      service-images
#      banner-images
#      garage-logos
# 5. Copier les clés depuis Dashboard → Settings → API
# 6. Mettre à jour NEXT_PUBLIC_GARAGE_ID avec l'UUID réel du garage
# 7. Sur Vercel : Project Settings → Environment Variables → ajouter les mêmes
#
# ══════════════════════════════════════════════════════════════════════
